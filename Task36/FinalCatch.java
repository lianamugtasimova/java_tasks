// final в catch - переменная e становится неизменяемой 
try {
    // какой-то код
} catch (final Exception e) {
    // e = new Exception(); // ошибка - e нельзя переприсвоить
    
    // final гарантирует, что переменная e не будет изменена
    // внутри блока catch
}